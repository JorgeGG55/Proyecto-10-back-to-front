const MainContent = () => {
  return `
      <div id="contenido-principal">
          <!-- El contenido de los eventos se cargará aquí -->
      </div>

      <div id="contenido-eventos">
      </div>
    `;
};

export default MainContent;
